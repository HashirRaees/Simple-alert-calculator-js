var fNumber = +prompt("Enter the first number to add:")
var sNumber = +prompt("Enter the second number to add:")
alert(fNumber + sNumber)

var fNumber = +prompt("Enter the first number to subtract:")
var sNumber = +prompt("Enter the second number to subtract:")
alert(fNumber - sNumber)

var fNumber = +prompt("Enter the first number to multiply:")
var sNumber = +prompt("Enter the second number to multiply:")
alert(fNumber * sNumber)

var fNumber = +prompt("Enter the first number to divide:")
var sNumber = +prompt("Enter the second number to divide:")
alert(fNumber / sNumber)

alert("Thanks")